namespace ETLBox.Connection
{
    public enum ConnectionManagerType
    {
        Unknown,
        SqlServer,
        Adomd,
        SQLite,
        MySql,
        Postgres,
        Access,
        Oracle
    }
}
